export declare function match(glob: string, input: string): boolean;
export declare function filterPaths(globs: string[], files: string[]): boolean;
export declare function filterBranch(globs: string[], input: string): boolean;
